import { Button, Typography } from '@mui/material';
import React from 'react';

const DemoComponent = () => {
    return (
        <div>
            <Typography>this is demo component</Typography>
            <Button variant="contained">This is demo material ui button</Button>
        </div>
    );
};

export default DemoComponent;